# $Id: build.config,v 1.2 2004/06/07 15:39:31 christian Exp $
project.type = library
project.prefix = y60
project.name = glutil

source.files = **/*.cpp
header.files = **/*.h
test.sources = **/*.tst.cpp

# frameworks.osx = OpenGL 

extra.include.dir.osx = /usr/X11R6/include

variant.libraries          = paintlib, aslbase, domxml, aslzip, y60base, y60scene, y60image, y60video 
external.libraries.linux   =
external.libraries.windows =
external.libraries.osx     = X11, GL, GLU

variant.test.libraries          = aslbase, domxml, aslzip, y60base, y60scene, y60image, y60video
external.test.libraries         = Cg, CgGL
external.test.libraries.linux   = pthread, rt, GL, GLU, tiff, curl, jpeg
external.test.libraries.windows = OpenGL32, GLU32, pthreadVC, advapi32, shell32

exclude.source.files.windows =
exclude.source.files.linux =

exclude.test.sources.windows =
