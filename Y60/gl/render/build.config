# $Id: build.config,v 1.19 2005/04/08 18:39:22 christian Exp $
project.type = library
project.prefix = y60
project.name = render

source.files = **/*.cpp
header.files = **/*.h
test.sources = **/*.tst.cpp

frameworks.osx = Cg, OpenGL, Carbon 

# temporarily disable CG on osx until Nvidia fixes it's stuff
# extra.defines.osx = _AC_NO_CG_

variant.libraries          = aslbase, aslmath, asldom, y60scene, y60base, y60glutil, y60gltext, aslzip, \
                                  y60image, y60video, paintlib

external.libraries.osx = avcodec, avformat, GLEW

variant.test.libraries          = aslbase, aslmath, asldom, y60scene, y60base, y60glutil, y60gltext, aslzip, \
                                  y60image, y60video, paintlib
external.test.libraries = avcodec, avformat, freetype 
external.test.libraries.linux   = pthread, rt , png, tiff, jpeg, gif, GL, GLU, GLEW, curl, asound, Cg, CgGL 
external.test.libraries.windows = libpng, libtiff, libjpeg, libungif, OpenGL32, GLU32, libcurl, \
                                  pthreadVC, gdi32, user32, advapi32, shell32, \
                                  libgcc, dxguid, dsound, Imath, Cg, CgGL, glew 

exclude.source.files.windows =
exclude.source.files.linux =
exclude.source.files.osx =
 #CgProgramInfo.h CgProgramInfo.cpp CgShader.h CgShader.cpp SkinAndBonesShader.h SkinAndBonesShader.cpp

exclude.test.sources.windows =
# exclude.test.sources.osx = testCgShaders.tst.cpp

schema.files = ShaderLibrary.xsd

