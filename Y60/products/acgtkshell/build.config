## Name of the library/application
project.type = application
#project.subtype = winapp
project.name = acgtkshell
project.prefix = y60

platform.compiler.options.linux = ${pkg.pkg-config.cflags} ${pkg.paintlib-config.cflags}
platform.linker.options.linux = ${pkg.pkg-config.libs} ${pkg.paintlib-config.libs}

extra.sysinclude.dir =
extra.sysinclude.dir.linux =

#extra.sysinclude.dir.linux=/usr/include/gtkmm-2.4;/usr/lib/gtkmm-2.4/include;\
#                        /usr/include/glibmm-2.4;/usr/lib/glibmm-2.4/include;\
#                        /usr/include/gdkmm-2.4;/usr/lib/gdkmm-2.4/include;\
#                        /usr/include/gtk-2.0;/usr/lib/gtk-2.0/include/;\
#                        /usr/include/glib-2.0;/usr/lib/glib-2.0/include;\
#                        /usr/include/sigc++-2.0/;/usr/lib/sigc++-2.0/include/;\
#                        /usr/include/pango-1.0/;/usr/include/atk-1.0/;\
#                        /usr/include/pangomm-1.4;/usr/include/atkmm-1.6;\
#                        /usr/include/libglade-2.0;/usr/include/libglademm-2.0;\
#                        /usr/include/gtkglext-1.0;/usr/lib/gtkglext-1.0/include;\
#                        myspace separated dir ectory;



extra.sysinclude.dir.windows =


# Comma separated list of Names of
# external libraries
external.libraries = png, tiff, jpeg, paintlib, Cg, CgGL

external.libraries.windows = opengl32, glu32, shell32, libcurl, gdi32, user32, \
    Netapi32, pthreadVC, freetype200MT, \
    advapi32, gtk-win32-2.0, gdk-win32-2.0, glib-2.0, \
    gobject-2.0, gdkglext-win32-1.0, gtkglext-win32-1.0, ws2_32, \
    gtkmm, gdkmm, pangomm, atkmm, glibmm, glade-2.0, libglademm, sigc, libgcc
    

external.libraries.linux = GLU, GL, curl, ungif

# external.libraries.linux = GLU, GL, pthread, curl, ungif, asound, freetype, \
#                           paintlib, gtk-x11-2.0, gdk-x11-2.0, glib-2.0, gdkglext-x11-1.0, gtkglext-x11-1.0, \
#                            gtkmm-2.4, gdkmm-2.4, pangomm-1.4, atkmm-1.6, glibmm-2.4, glademm-2.0, sigc-2.0

# Comma separated list of Names of variant libraries
variant.libraries = aslbase, aslmath, aslserial, aslnet, domxml, aslipc, \
                    y60scene, y60base, y60image, y60gltext, y60render, \
                    y60input, y60inet, jsgtkbase, \
                    y60jslib, y60glutil, y60video, \
                    jsspidermonkey, y60modelling, aslzip, acgtkWidgets

# Comma separated list of Names/Patterns of all source files
source.files = **/*.cpp, **/*.c++, **.c
source.files.windows =
source.files.linux =
exclude.source.files = **/*.tst.cpp, **/*.tst.c++
exclude.source.files.windows =
exclude.source.files.linux =
release.compiler.options.windows = /Zi

# Comma separated list of Names/Patterns of header files to install
header.files = **/*.h
exclude.header.files =

# Comma separated list of Names/Patterns of all test sources
test.sources = xpshell.cpp **/*.tst.cpp, **/*.tst.c++
test.sources.windows =
test.sources.linux =
exclude.test.sources = acgtkshell.cpp
exclude.test.sources.windows =
exclude.test.sources.linux =

# Comma separated list of variant libraries for tests
variant.test.libraries = aslbase, aslmath, aslserial, aslnet, domxml, y60inet, y60render, y60scene, \
                    audiobase, audioapp, y60input, y60glutil, \
                    y60gltext, y60jslib, sdlttf2, jsspidermonkey, y60modelling

# Comma separated list of external libraries for tests
external.test.libraries = png, tiff, jpeg, paintlib, GL, GLU, Cg, CgGL, avcodec, avformat
external.test.libraries.windows = SDL, SDLmain, libcurl, gdi32, user32, pthreadVC, winmm, freetype200MT
external.test.libraries.linux = pthread, curl, ungif, asound
