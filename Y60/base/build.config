# $Id: build.config,v 1.6 2005/03/24 23:35:56 christian Exp $
project.type = library
project.prefix = y60
project.name = base

source.files = **/*.cpp
exclude.source.files = **/*.tst.cpp
header.files = **/*.h
test.sources = **/*.tst.cpp

variant.libraries          = aslbase, domxml, aslmath
external.libraries         = Half
# the osx linker needs these libraries to be listed here
external.libraries.osx = png, tiff, jpeg, curl, z, avformat, avcodec
external.libraries.linux = ungif, png
external.libraries.windows = Imath

variant.test.libraries          = aslbase, domxml, aslmath, audiobase, audioapp, aslzip
external.test.libraries.linux   = pthread, curl, asound
external.test.libraries.windows = pthreadVC, libcurl, gdi32, user32, ole32, advapi32, shell32, libgcc, dxguid, dsound
external.test.libraries         = png, tiff, z, jpeg, ungif, avcodec, avformat

#debug.linker.options.osx = -read_only_relocs warning
#release.linker.options.osx = -read_only_relocs warning

exclude.source.files.windows =
exclude.source.files.linux = WMVDecoder.*

exclude.test.sources = testFileImage.tst.cpp
exclude.test.sources.windows =

schema.files = Y60.xsd

