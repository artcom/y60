project.type = shared-library
project.prefix = 
project.name = gl

frameworks.osx = OpenGL, Carbon, Cg 
extra.include.dir.osx = /usr/X11R6/include

variant.libraries = aslbase, aslmath, aslserial, aslnet, aslipc, asldom, aslxpath, aslzip, y60scene, y60base, y60image, y60video, y60jsbase, y60gltext,  \
                    y60inet, y60input, y60jslib, y60render, y60scene, y60image, y60glutil, y60modelling, y60sdlengine, jsspidermonkey, paintlib 

external.libraries.windows = libpng, libtiff, libjpeg, libungif, opengl32, glu32, shell32, libcurl, \
                             gdi32, user32, Netapi32, pthreadVC, winmm, \
                             ws2_32, libfreetype, advapi32, libgcc, dxguid, zdll \
                             Imath, Cg, CgGL, Iphlpapi

					
source.files = **/*.cpp
header.files = **/*.h
