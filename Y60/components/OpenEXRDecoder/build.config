# Subdirectory names
subdirs =

# Name of the library/application
project.name = OpenEXR
# only for libraries & xpcom components:
project.prefix = y60

# Project types are:
#   - application
#   - static-library
#   - shared-library
project.type = shared-library

# Comma separated list of
# extra defines
extra.defines =
extra.defines.linux =
extra.defines.windows = PLATFORM_WINDOWS

# SEMICOLON(!) separated list of
# extra include dirs
extra.include.dir = ${env.PRO}/include/OpenEXR


external.libraries = png, tiff, jpeg, IlmImf, Imath, Iex, Half
external.libraries.windows = user32, Shell32, advapi32, pthreadVC, libcurl, zdll
external.libraries.linux = pthread, curl, z, 
variant.libraries = aslbase, paintlib

# Comma separated list of Names/Patterns of
# all source files
source.files = **/*.cpp, **/*.c++
source.files.windows =
source.files.linux =
exclude.source.files = **/*.tst.cpp, **/*.tst.c++
exclude.source.files.windows =
exclude.source.files.linux =

# Comma separated list of Names/Patterns of
# header files to install
header.files = **/*.h
exclude.header.files =

# Comma separated list of Names/Patterns of
# all test sources
test.sources =
test.sources.windows =
test.sources.linux =
exclude.test.sources =
exclude.test.sources.windows =
exclude.test.sources.linux =

# Comma separated list of
# external libraries for tests
external.test.libraries = pthread
external.test.libraries.windows =
external.test.libraries.linux =

# Comma separated list of
# variant libraries for tests
variant.test.libraries = aslbase

