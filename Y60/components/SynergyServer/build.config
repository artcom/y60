project.type = shared-library
project.prefix =
project.name = SynergyServer

variant.libraries = aslbase, aslnet
external.libraries = pthread

variant.test.libraries = y60jsbase, y60jslib
external.test.libraries.linux = jpeg, tiff, Cg, CgGL

source.files = **/*.cpp
header.files = **/*.h

