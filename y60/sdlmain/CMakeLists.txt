ac_add_library(
    y60_sdlmain y60/sdlmain
    DEPENDS aslbase
    SOURCES SDLMain.m
    HEADERS SDLMain.h
    EXTERNS SDL
    STATIC
    VERSION ${VERSION_STRING}
    SOVERSION ${VERSION_MAJOR}
    RUNTIME_INSTALL_COMPONENT ${RUNTIME_COMPONENT}
    DEVELOPMENT_INSTALL_COMPONENT ${DEVELOPMENT_COMPONENT}
)
    
